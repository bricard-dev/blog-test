import Section from "@/components/layout/section";
import HeroCarousel from "./carousel";

export default function HeroSection() {
  return (
    <Section fullHeight className="p-0">
      <HeroCarousel />
    </Section>
  );
}
